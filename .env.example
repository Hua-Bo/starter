# ============================================================
# 环境变量模板
# 使用方式：复制为 .env.development（本地）或 .env.production（生产）
# 注意：VITE_ 前缀的变量会打包进前端代码，切勿存放密钥
# ============================================================

# 站点根路径
# - 本地开发、Cloudflare、自定义域：/
# - GitHub Pages 项目页：/仓库名/（Actions 会自动设置，本地无需改）
VITE_BASE=/

# 浏览器标签页标题，同时用于路由 meta.title 的默认值
VITE_APP_TITLE=Vue Starter

# 可选：后端 API 基址
# 部署到 Cloudflare 后可填 Worker 地址，例如 https://my-app.xxx.workers.dev
VITE_API_BASE=
